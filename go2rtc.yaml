streams:
  camara_principal:
    # Path RTSP especifico de Tapo (stream2 = H.264, baja resolucion).
    # Si usas otra marca, cambia el path segun la tabla del README.
    - rtsp://${CAMERA_USER}:${CAMERA_PASSWORD}@${CAMERA_IP}:554/stream2

webrtc:
  listen: ":8555"

api:
  listen: ":1984"
